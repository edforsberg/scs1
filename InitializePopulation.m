function [positions, states] = InitializePopulation(populationSize, nrInfected, canvasSize)

states = Outbrake(populationSize, nrInfected); 

positions = zeros(populationSize, 2); 

for i = 1:populationSize
    
    x = randi(100); 
    if 
    positions(i,:) = 
end 

end

